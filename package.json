{
  "name": "epic-chess",
  "version": "1.0.0",
  "description": "Epic Chess - jeu d'echecs variant, HTML/CSS/JS pur, sans framework ni build step.",
  "private": true,
  "scripts": {
    "dev": "npx --yes serve -l 8080 .",
    "start": "npx --yes serve -l 8080 ."
  },
  "engines": {
    "node": ">=16"
  }
}